import * as types from '../types';

const initialState = {
   userId: undefined
}

export default function jobSolutions(state = initialState, action) {
   switch (action.type) {

      default:
      return state
   }
} 